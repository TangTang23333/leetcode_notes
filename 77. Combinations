77. Combinations
Medium

2934

97

Add to List

Share
Given two integers n and k, return all possible combinations of k numbers out of the range [1, n].

You may return the answer in any order.


class Solution:
    def combine(self, n: int, k: int) -> List[List[int]]:
        self.res = []
        if not k:
            return self.res 
        if n == 1:
            return [[1]]
        
        
        self.dfs(n, k, 1, [])
        return self.res
    
    
    
    
    
    def dfs(self, n, k, start, subset):
        # exit 
        if len(subset) == k:
            self.res.append(subset[:])
            return 
        
        for i in range(start, n+1):
            # choose start 
            self.dfs(n, k, i+1, subset + [i])
            
